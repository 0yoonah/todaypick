# 변경 내역

이 프로젝트는 [Semantic Versioning](https://semver.org/lang/ko/)을 따릅니다.

## [1.2.0] - 배포 예정

### 추가

- Supabase 스키마, RLS, Storage 정책 마이그레이션과 환경 변수 예시
- 날짜 경계, 연속 학습, 스크랩 캐시 동작 단위 테스트
- pull request와 주요 브랜치에서 lint, type-check, test, build를 실행하는 CI
- 프로필 이미지 명시적 제거와 안전한 교체 흐름

### 변경

- Next.js 15에서 16으로, React 19.1에서 19.2로 업그레이드
- 일별 콘텐츠와 학습 통계의 날짜 기준을 `Asia/Seoul`로 통일
- RSS 수집 결과를 제한 시간과 15분 캐시가 적용된 고정 목록으로 페이지네이션
- 연속 학습일을 실제로 이어지는 학습 날짜 기준으로 계산
- 스크랩 추가·해제 후 관련 목록을 즉시 갱신

### 수정

- 닉네임만 수정할 때 기존 프로필 이미지가 사라지는 문제
- 같은 날의 학습 활동을 기록할 때 기존 활동 값이 덮어써지는 문제
- 명언 스크랩 해제 시 식별자 혼용과 빈 상태 표시 문제
- 스크랩 탭에서 해제한 피드가 화면에 남는 문제

### 릴리스 검증

아래 명령이 모두 통과한 `master` 커밋에 `v1.2.0` 태그를 생성합니다.

```bash
npm ci
npm run lint
npm run type-check
npm test
npm run build
```
