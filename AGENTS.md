# TodayPick Agent Guide

이 문서는 TodayPick 저장소에서 작업하는 Codex 및 자동화 에이전트가 따라야 할 프로젝트 규칙을 정의한다.

## 적용 범위

- 이 파일은 저장소 전체에 적용한다.
- 하위 디렉터리에 별도의 `AGENTS.md`가 있다면 해당 디렉터리에서는 더 가까운 파일의 규칙을 우선한다.
- 사용자 요청과 시스템·개발자 지침이 이 문서보다 우선한다.

## 프로젝트 개요

- Next.js App Router 기반의 TypeScript 웹 애플리케이션이다.
- TanStack Query로 서버 상태를 관리한다.
- Zustand로 인증 상태를 관리한다.
- Supabase Auth, Database, Storage와 RLS를 사용한다.
- 스타일링은 Tailwind CSS와 프로젝트의 `src/components/ui` 컴포넌트를 우선 사용한다.
- 배포 환경은 Vercel이다.
- 사용자 날짜와 주간 통계는 `Asia/Seoul`을 기준으로 한다.

## 브랜치 운영

### 기준 방향

- `develop`을 기능 개발과 수정 작업의 통합 브랜치로 사용한다.
- 새로운 작업을 시작하기 전에 로컬 `develop`을 `origin/develop`과 동기화한다.
- 기능·수정 작업은 최신 `develop`에서 이슈별 브랜치를 생성해 진행한다.
- 이슈 작업 PR의 base는 `develop`으로 지정한다.
- `master`는 운영 배포와 릴리스의 기준 브랜치로 사용한다.
- 릴리스 범위의 작업이 `develop`에 모두 반영되면 `develop → master` 릴리스 PR을 생성한다.
- 사용자 요청이 있는 긴급 수정, 운영 설정, 문서 작업이 아니라면 `master`에서 직접 작업하지 않는다.
- 사용자 요청으로 `master`에 직접 반영한 변경은 완료 후 `master → develop`으로 동기화한다.

### 작업 브랜치

- 한 이슈에는 하나의 독립 브랜치를 사용한다.
- 여러 이슈의 변경을 하나의 브랜치나 PR에 섞지 않는다.
- 각 이슈 브랜치는 최신 `develop`에서 독립적으로 생성하며 다른 작업 브랜치에서 이어서 만들지 않는다.
- 브랜치 이름에는 이슈 번호와 목적을 포함한다.

예시:

```text
feat/20-daily-learning-checklist
fix/31-statistics-query
chore/32-release-config
```

### 병합과 동기화

1. 최신 `develop`에서 이슈 브랜치를 생성한다.
2. 이슈 작업과 검증을 완료한다.
3. 이슈 브랜치에서 `develop` 대상 PR을 생성한다.
4. GitHub Actions `CI / verify`와 Vercel Preview가 통과한 뒤 `develop`에 병합한다.
5. 릴리스 범위의 모든 이슈 PR이 병합될 때까지 같은 흐름을 반복한다.
6. `develop → master` 릴리스 PR을 생성한다.
7. GitHub Actions `CI / verify`와 Vercel 검사가 통과한 뒤 `master`에 병합한다.
8. Production을 확인하고 검증된 `master` 병합 커밋에 태그와 GitHub Release를 생성한다.
9. 병합 완료 작업 브랜치를 정리한다.

- 각 이슈 PR의 base는 `develop`으로 유지하고 다른 이슈 브랜치를 base로 지정하지 않는다.
- PR base를 변경했다면 변경 범위가 해당 이슈 파일만 포함하는지 다시 확인한다.
- base 변경은 기본 `pull_request` GitHub Actions를 자동 실행하지 않을 수 있다. 필요하면 PR을 닫았다가 다시 열어 `reopened` 이벤트로 검증을 실행한다.

### 브랜치 정리

- 저장소의 `Automatically delete head branches` 설정이 켜져 있어 **원격 작업 브랜치는 PR 병합과 함께 자동으로 삭제된다.** 병합 후 별도로 `git push origin --delete`를 실행하지 않는다.
- 자동 삭제 대상은 **병합된 PR의 head 브랜치**다. 릴리스 PR은 `develop → master`이므로 **`develop`도 head가 된다.** 이 설정만 켜 두면 릴리스 PR을 병합할 때 `develop`이 삭제된다.
- 그래서 `develop`과 `master`에 브랜치 보호를 적용해 두었다. GitHub는 보호된 브랜치를 자동 삭제하지 않는다.
  - 삭제 금지와 강제 푸시 금지만 설정했고, 리뷰 필수나 상태 검사 필수는 걸지 않았다.
  - 보호를 해제하면 다음 릴리스 병합에서 `develop`이 사라질 수 있다. 해제할 일이 있으면 자동 삭제 설정을 함께 확인한다.
- `master`, `develop`과 명시적인 보관 브랜치(`archive/*`)는 수동으로도 삭제하지 않는다.
- 로컬 브랜치는 자동으로 정리되지 않는다. 병합을 확인한 뒤 직접 정리한다.

```bash
git checkout develop
git pull --ff-only
git fetch --prune          # 원격에서 사라진 추적 참조 정리
git branch -d <브랜치>      # 병합되지 않았으면 삭제되지 않는다
```

- 브랜치를 삭제하기 전에 대상 커밋이 의도한 기준 브랜치에 병합됐는지 확인한다. 이슈 브랜치는 `develop`, 릴리스 변경은 `master` 반영 여부를 기준으로 한다.
- 자동 삭제가 꺼져 있거나 과거에 쌓인 브랜치를 정리할 때는 두 조건을 함께 확인한다.
  - `git branch -r --contains <브랜치>`에 `origin/develop` 또는 `origin/master`가 포함된다.
  - `git rev-list --count origin/develop..<브랜치>`가 `0`이다.
- `git branch -D`처럼 병합 여부를 무시하는 강제 삭제는 사용하지 않는다.
- 병합되지 않은 커밋이 남은 브랜치는 임의로 지우지 않고 처리 방식을 먼저 정한다.

기준 브랜치가 삭제됐다면 로컬 참조로 복구한다. 원격에 없던 커밋이 로컬에도 없는지 먼저 확인한다.

```bash
git ls-remote --heads origin        # 원격에 남은 브랜치 확인
git log --oneline develop -1        # 로컬 참조가 가리키는 커밋 확인
git push origin develop:develop     # 복구
```

## 이슈와 PR

- 작업 전에 GitHub 이슈 본문과 완료 조건을 읽는다.
- 구현 PR 하나에는 이슈 하나만 연결한다.
- PR 제목에는 변경 유형, 핵심 내용, 이슈 번호를 포함한다.

예시:

```text
feat: 오늘의 학습 체크리스트 추가 (#20)
```

PR 본문에는 최소한 다음 내용을 작성한다.

- 배경과 해결하려는 문제
- 사용자에게 보이는 변경 사항
- 주요 구현 기준과 계산 규칙
- API 또는 데이터 구조 변경
- Supabase migration 목록과 실제 적용 필요 여부
- 검증 명령과 결과
- 알려진 제한 사항
- 관련 이슈

- `develop` 대상 구현 PR에는 `Related to #번호`로 작업 관계를 남긴다.
- `master` 대상 릴리스 PR에는 이번 릴리스로 완료되는 이슈를 각각 `Closes #번호`로 명시한다.
- 여러 이슈의 구현을 하나의 PR에 합치지 않는다. 단, 릴리스 PR은 개별 검증과 병합을 마친 여러 이슈를 종료할 수 있다.
- 후속 작업이 남아 실제로 완료되지 않은 이슈에는 `Closes`를 사용하지 않는다.

## 커밋

- 커밋은 하나의 논리적 변경 단위로 작성한다.
- 관련 없는 파일을 함께 커밋하지 않는다.
- 커밋 메시지는 Conventional Commits 형식을 사용한다.
- 가능하면 커밋 메시지 끝에 이슈 번호를 포함한다.

예시:

```text
feat: 관심 분야 기반 맞춤 피드 추가 (#21)
fix: 기존 users 테이블 스키마 보정 (#21)
style: 학습 체크리스트 너비 통일 (#20)
```

## 필수 검증

코드 변경 후 다음 검증을 실행한다.

```bash
npm run lint
npm run type-check
npm test
npm run build
```

- 작은 문서 변경을 제외하면 lint와 관련 테스트를 생략하지 않는다.
- API, 타입, 빌드 설정, Next.js metadata를 수정했다면 production build를 실행한다.
- 화면 변경은 데스크톱과 모바일에서 확인한다.
- 로딩, 오류, 빈 데이터, 비로그인 상태를 함께 확인한다.
- GitHub Actions `CI / verify`와 Vercel Preview가 통과했는지 확인한다.
- Google Fonts 네트워크 제한 때문에 build가 실패하면 승인된 네트워크 환경에서 같은 build를 다시 실행한다.
- 브랜치 전환 후 `.next` 타입 캐시가 다른 브랜치의 경로를 참조하면 생성 캐시를 안전하게 치우고 현재 브랜치 기준으로 다시 검증한다.

## Next.js와 React

- Server Component를 기본으로 사용하고 브라우저 상태나 이벤트가 필요한 경우에만 `"use client"`를 추가한다.
- 기존 App Router 파일 규칙과 metadata API를 따른다.
- `themeColor`는 metadata가 아니라 `Viewport` export에 설정한다.
- 앵커 스크롤에는 모션 감소 환경을 고려한다.
- `scroll-behavior: smooth`를 사용하면 Next.js 안내에 맞게 필요한 HTML 속성도 확인한다.
- 개발 모드의 React Strict Mode에서 effect가 두 번 실행될 수 있음을 고려한다.
- 조회 요청은 TanStack Query를 우선 사용해 캐시와 중복 요청을 관리한다.
- POST, PUT, DELETE와 같은 상태 변경 API는 반복 요청에도 데이터가 깨지지 않도록 멱등성을 보장한다.
- effect에서 네트워크 요청을 수행하면 cleanup 또는 취소 가능성을 고려한다.

## 상태 관리와 API

- 서버 데이터는 TanStack Query 쿼리 키와 무효화 범위를 명확히 정의한다.
- mutation 성공 후 관련 화면이 새로고침 없이 갱신되도록 캐시를 갱신하거나 무효화한다.
- 낙관적 업데이트를 사용할 때 실패 시 이전 캐시를 복원한다.
- API는 인증, 입력 검증, 정상 응답, 오류 응답을 구분한다.
- 서버 로그에는 원인을 확인할 수 있는 오류를 남기되 토큰, 비밀번호, 개인정보는 출력하지 않는다.
- 클라이언트에 내부 DB 오류를 그대로 노출하지 않는다.
- 사용자 입력 배열은 허용된 값만 파싱하고 중복을 제거한다.

## 날짜와 통계

- 사용자에게 보이는 날짜 키는 `src/utils/dateUtils.ts`의 서울 시간 유틸리티를 사용한다.
- 클라이언트가 보낸 날짜를 신뢰해 일일 활동 날짜를 결정하지 않는다.
- 서버에서 `Asia/Seoul` 기준 날짜를 계산한다.
- 한 주는 서울 시간 기준 월요일부터 일요일까지다.
- UTC 타임스탬프를 일별·주별 통계에 사용할 때 서울 날짜 키로 변환한다.
- 자정, 월말, 윤년, 연말 경계를 테스트한다.
- 통계 집계 기준은 PR 본문과 타입에 명확히 기록한다.
- 같은 데이터를 읽기와 저장처럼 여러 경로에서 집계할 때 고유 식별자 기준의 중복 여부를 명시한다.

## 콘텐츠 데이터

- 모든 사용자에게 같은 값인 콘텐츠는 `src/data`의 정적 파일로 관리하고, 사용자마다 다른 기록만 DB에 저장한다.
- 콘텐츠 파일에는 정합성 테스트를 함께 작성한다. id 중복, 필수 값, 참조 유효성, 분류 범위를 확인한다.
- 사용자 기록이 콘텐츠 id를 참조하므로 기존 id를 바꾸지 않는다. 분류나 문구가 바뀌어도 id는 유지한다.
- `created_at`처럼 고정돼야 하는 값에 `new Date()`를 쓰지 않는다.
- 콘텐츠 목록은 서버 컴포넌트에서 렌더링한다. 클라이언트 컴포넌트가 `src/data`를 직접 import하면 데이터 전체가 번들에 포함된다.
- DB 전환 여부를 판단할 때는 [docs/content-data-policy.md](docs/content-data-policy.md)의 기준을 따른다.

## 문서

- 사용자에게 보이는 기능을 추가하거나 없애거나 동작을 바꾸면 같은 PR에서 `README.md`를 함께 갱신한다. 문서 갱신을 다음 작업으로 미루지 않는다.
- 기능 변경 시 `README.md`에서 확인할 항목은 다음과 같다.
  - 주요 기능 섹션
  - 프로젝트 구조의 디렉터리 목록과 설명
  - 주요 테이블과 주요 함수 표
  - 주요 페이지(라우트) 표
  - 문서 링크 목록
- `README.md`에 적는 수치와 라벨은 코드에서 확인한 값만 쓴다. 문항 수, 용어 수, 분류 라벨, 탭 이름, 기준 점수 같은 값은 실제 상수나 데이터 파일과 대조한다.
- 없어진 기능은 `README.md`에서도 지운다. 구현되지 않은 기능을 있는 것처럼 적지 않는다.
- 운영 절차나 판단 기준이 생기면 `docs/`에 문서를 만들고 `README.md`의 문서 목록과 관련 규칙에 링크한다. 이미 링크된 문서를 중복해서 추가하지 않는다.
- `AGENTS.md`의 규칙이 실제 작업 방식과 달라졌다면 규칙을 먼저 고치고 그 규칙대로 작업한다.

## Supabase와 Migration

- 스키마 변경은 `supabase/migrations`의 새 migration 파일로 관리한다.
- 이미 원격에 적용된 migration 파일을 수정해 재실행을 기대하지 않는다.
- 적용된 migration을 보정해야 하면 더 늦은 타임스탬프의 새 migration을 추가한다.
- `create table if not exists`만으로 기존 테이블의 누락 컬럼이 보정된다고 가정하지 않는다.
- 기존 운영 스키마와 신규 환경 양쪽을 고려해 `add column if not exists`와 필요한 트리거 보정을 작성한다.
- 테이블을 추가하면 필요한 인덱스, unique 제약, RLS 활성화와 정책을 함께 검토한다.
- 사용자 데이터는 기본적으로 본인만 접근할 수 있어야 한다.
- migration 파일 생성과 원격 DB 적용은 별개의 작업이다.
- migration을 실제 적용하지 않았다면 적용했다고 보고하지 않는다.
- 원격 적용 전 pending migration을 확인하고, 사용자 승인 없이 운영 DB를 변경하지 않는다.
- PR 본문과 Release notes에 필요한 migration 파일과 적용 순서를 명시한다.

## UI와 접근성

- 주요 페이지의 콘텐츠 영역은 `max-width: 1080px`과 동일한 좌우 시작선을 사용한다.
- 색상은 `src/app/globals.css`의 역할 기반 토큰을 사용하고 임의의 hex나 Tailwind 색상 단계를 직접 추가하지 않는다.
- 모든 콘텐츠를 카드로 감싸지 않고 여백과 구분선으로 정보 위계를 만든다.
- 기존 페이지의 최대 너비, 간격, 카드 스타일과 시각적 위계를 맞춘다.
- 동일한 행동을 유도하는 CTA를 한 화면에 불필요하게 중복하지 않는다.
- 장식적인 그라데이션은 명확한 목적이 있을 때만 사용한다.
- 버튼과 링크에는 명확한 accessible name을 제공한다.
- 진행률에는 `progressbar` 역할과 현재 값을 제공한다.
- 색상만으로 상태를 전달하지 않고 아이콘, 숫자 또는 텍스트를 함께 사용한다.
- 모바일에서 제목이 어색하게 끊기거나 컨트롤이 넘치지 않는지 확인한다.
- 모바일 고정 내비게이션은 기기 safe area와 콘텐츠 하단 가림을 함께 고려한다.
- 모바일에서 카드 높이, 터치 영역, 가로 스크롤과 320px 너비의 레이아웃을 확인한다.
- `prefers-reduced-motion`을 존중한다.
- 로딩, 오류, 빈 상태가 서로 구분되어야 한다.

## RSS와 맞춤 피드

- RSS 수집 실패가 전체 피드 요청 실패로 번지지 않도록 소스 단위로 격리한다.
- URL을 정규화한 뒤 중복을 제거한다.
- 전체 목록의 중복 제거와 결정적 정렬을 완료한 후 페이지를 나눈다.
- 같은 입력에 같은 순서가 나오도록 마지막 정렬 키를 둔다.
- 개인화 결과가 부족해도 비개인화 피드를 제거하지 않고 fallback으로 유지한다.
- 분류 키워드 변경 시 성능뿐 아니라 짧은 키워드의 오분류 가능성을 검토한다.
- 피드 분류·정렬 규칙을 변경하면 우선순위, fallback, 동일 점수 정렬 테스트를 추가한다.

## 릴리스

### 마일스톤과 릴리스 이슈

- 릴리스 작업을 시작할 때 GitHub에 버전 마일스톤을 생성한다.
- 마일스톤 이름은 Git tag와 같은 `vMAJOR.MINOR.PATCH` 형식을 사용한다.
- 마일스톤에는 목표 버전, 핵심 목표, 포함 범위와 제외 범위를 기록한다.
- 해당 버전에 포함할 이슈를 마일스톤에 연결한다.
- 릴리스 전체 진행 상황을 관리하는 별도의 릴리스 이슈를 생성하고 같은 마일스톤에 연결한다.
- 릴리스 이슈에는 다음 체크리스트를 포함한다.

```md
## 릴리스 체크리스트

- [ ] 마일스톤 대상 구현 이슈의 작업 및 `develop` 병합 완료
- [ ] 각 이슈 PR의 `develop` 병합 및 필수 검사 통과
- [ ] DB migration 적용 순서와 운영 반영 여부 확인
- [ ] `README.md`가 이번 릴리스의 기능·구조·라우트·테이블과 일치하는지 확인
- [ ] package 버전 변경
- [ ] `develop → master` 릴리스 PR 생성
- [ ] 릴리스 PR GitHub Actions `CI / verify`와 Vercel 검사 통과
- [ ] `master` 병합 및 Vercel Production 배포 확인
- [ ] Git tag 생성
- [ ] GitHub Release와 릴리스 노트 게시
- [ ] 마일스톤 종료
```

- 마일스톤에 완료되지 않은 이슈가 남아 있으면 반드시 이번 릴리스에 포함할지 다음 버전으로 옮길지 결정한다.
- 다음 버전으로 미루는 이슈는 대상 마일스톤을 변경하고 릴리스 노트의 알려진 제한 사항에 필요한 내용을 남긴다.

### 버전과 릴리스 PR

- 버전은 Semantic Versioning을 따른다.
  - `PATCH`: 호환성을 깨지 않는 버그 수정
  - `MINOR`: 호환성을 유지하는 기능 추가
  - `MAJOR`: 기존 사용 방식이나 API와 호환되지 않는 변경
- `package.json`과 lockfile의 버전을 함께 변경한다.
- package 버전, `master` 병합 커밋, Git tag와 GitHub Release가 같은 버전을 가리키도록 확인한다.
- 마일스톤 대상 이슈가 `develop`에 모두 병합되면 `develop → master` 릴리스 PR을 생성한다.
- 릴리스 PR 제목은 `release: vMAJOR.MINOR.PATCH (#릴리스이슈번호)` 형식을 사용한다.
- 릴리스 PR 본문에는 포함된 변경, migration, 검증 결과, 배포 시 주의 사항과 완료된 기능 이슈의 `Closes #번호`를 작성한다.
- 릴리스 관리 이슈는 릴리스 PR에서 `Related to #번호`로 연결하고 자동 종료하지 않는다.
- 미적용 migration이나 필수 환경 변수 변경이 있다면 릴리스 PR을 병합하기 전에 적용 시점과 담당 절차를 명확히 한다.

### 배포, 태그와 GitHub Release

1. 릴리스 PR의 GitHub Actions `CI / verify`와 Vercel 검사가 통과했는지 확인한다.
2. 릴리스 PR을 `master`에 병합한다.
3. Vercel Production 배포가 성공하고 주요 사용자 흐름이 정상인지 확인한다.
4. 검증된 `master` 병합 커밋에 `vMAJOR.MINOR.PATCH` 태그를 생성한다.
5. 태그를 원격에 푸시하고 같은 태그로 GitHub Release를 생성한다.
6. GitHub Release 게시 후 릴리스 이슈와 마일스톤을 종료한다.

- Production 확인 전에는 태그와 GitHub Release를 만들지 않는다.
- 릴리스 PR을 병합한 뒤 `git ls-remote --heads origin`으로 `develop`이 남아 있는지 확인한다. 릴리스 PR은 head가 `develop`이라 브랜치 보호가 풀려 있으면 자동 삭제될 수 있다.
- 잘못된 커밋에 태그를 생성했다면 임의로 덮어쓰지 말고 사용자와 수정 방식을 먼저 결정한다.
- 릴리스 관리 이슈는 태그 생성, GitHub Release 게시와 Production 확인이 모두 끝난 뒤 체크리스트를 갱신하고 수동으로 종료한다.

### 릴리스 노트 템플릿

릴리스 노트는 커밋 목록을 그대로 나열하지 않고 사용자가 체감하는 변화와 운영에 필요한 내용을 중심으로 작성한다.

```md
# TodayPick vMAJOR.MINOR.PATCH

이번 릴리스의 목표와 가장 중요한 변화를 2~3문장으로 설명합니다.

## 주요 변경 사항

### 기능명 또는 사용자 가치

- 사용자가 할 수 있게 된 일과 기존 동작에서 달라진 점
- 중요한 동작 기준, 계산 규칙 또는 fallback 방식

## 개선 사항

- UI, 접근성, 성능, 안정성 또는 개발 환경 개선 내용

## 버그 수정

- 해결한 문제와 사용자에게 미치던 영향

## 데이터베이스 및 운영 반영

- migration 파일명과 적용 순서
- 실제 운영 DB 적용 여부
- 환경 변수, 외부 서비스 또는 배포 설정 변경
- 별도 작업이 없으면 `없음`

## 검증

- 실행한 lint, type-check, test와 production build 결과
- GitHub Actions `CI / verify`, Vercel Preview와 Production 확인 결과
- 직접 확인한 주요 사용자 흐름

## 알려진 제한 사항

- 이번 버전에 포함하지 않은 문제와 우회 방법
- 제한 사항이 없으면 `없음`

## 관련 이슈

- #이슈번호 이슈 제목

## 전체 변경 내역

- [v이전버전...v현재버전](GitHub compare URL)
```

- `주요 변경 사항`에는 구현 파일이나 내부 라이브러리보다 사용자 관점의 결과를 먼저 설명한다.
- 관심 분야 분류, 통계 집계처럼 결과 기준이 중요한 기능은 우선순위, 예외 처리와 fallback 규칙을 구체적으로 기록한다.
- migration은 파일 생성 여부와 실제 운영 적용 여부를 구분해 작성한다.
- 검증하지 않은 항목을 완료된 것처럼 표시하지 않는다.
- 릴리스 노트에는 이전 버전과 현재 버전 사이의 GitHub compare 링크를 포함한다.

## 안전

- 사용자 작업이나 관련 없는 변경을 덮어쓰지 않는다.
- dirty worktree에서는 관련 파일만 수정하고 스테이징한다.
- `git reset --hard`, 광범위한 삭제, 강제 푸시는 명확한 필요와 사용자 승인 없이 사용하지 않는다.
- force push가 필요하면 `--force-with-lease`를 사용한다.
- 비밀값을 터미널 출력, 커밋, PR, 로그에 노출하지 않는다.
- 외부 시스템의 병합, 배포, DB 적용, 태그·Release 생성은 요청 범위와 승인 여부를 확인한다.
